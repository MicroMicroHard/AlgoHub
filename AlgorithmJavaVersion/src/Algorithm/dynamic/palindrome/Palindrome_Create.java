package Algorithm.dynamic.palindrome;

/**
 * @author liujun
 * @version 1.0
 * @date 2022/7/5 23:38
 * @author—Email ljfirst@mail.ustc.edu.cn
 * @description 创建回文串
 * 通过操作，返回构造的回文串
 * @blogURL
 */

public class Palindrome_Create {

    // 给定一个字符串s，你可以从中 添加 一个或多个 字符，使得剩下的串是一个回文串
    public int createMethod_add(String params) {
        return 0;
    }

    // 给定一个字符串s，你可以从中删除一些字符，使得剩下的串是一个回文串。
    public int createMethod_delete(String params) {
        return 0;
    }

    public int createMethod_delete_plus(String params) {
        return 0;
    }
}
